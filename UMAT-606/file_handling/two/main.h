#ifndef STD
  #include <stdio.h>
  #include <stdlib.h>
#endif // !STD
#ifndef FILE_HANDLING

/*
 * Compares the content of two files and prints the first different line
 *
 * @param f1: FILE poitner opened in `r` mode
 *
 * @param f2: FILE poitner opened in `r` mode
 */
  void compareFiles(FILE* f1, FILE* f2);
   

#endif // !FILE_HANDLING
